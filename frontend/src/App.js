import React, { Component } from "react";
import { Switch, Route, Redirect } from "react-router";

const App = () => {
   return (
      <div className="app">
          <Switch>
              
          </Switch>
      </div>
   );
};

export default App;